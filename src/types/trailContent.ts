export type ItemStatus = 'locked' | 'current' | 'completed' | 'failed';

export interface TimelineTheme {
  color: string;
  bg: string;
  borderColor: string;
  icon: string;
  lightColor: string;
}
